<Window x:Class="PL.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="800"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Closed="Window_Closed"
           Loaded="Window_Loaded" >

    <Grid>

        <Border BorderBrush="Blue" BorderThickness="1" Margin="304,39,147,424">

        </Border>

        <!--Internal, Upper Grid-->

        <Label Content="{Binding CurrentTime}" HorizontalAlignment="Left" Margin="422,43,0,0" VerticalAlignment="Top" RenderTransformOrigin="-0.667,0.548"/>
        <Button Content="Add One Minute" HorizontalAlignment="Left" Margin="400,77,0,0" VerticalAlignment="Top" Height="20" Width="147" Click="btnAddOneMinute_Click"/>
        <Button Content="Add One Hour" HorizontalAlignment="Left" Margin="400,97,0,0" VerticalAlignment="Top" Height="20" Width="147" Click="btnAddOneHour_Click"/>
        <Button Content="Add One Day" HorizontalAlignment="Left" Margin="400,117,0,0" VerticalAlignment="Top" Height="20" Width="147" Click="btnAddOneDay_Click"/>
        <Button Content="Add One Month" HorizontalAlignment="Left" Margin="400,137,0,0" VerticalAlignment="Top" Height="20" Width="147" Click="btnAddOneMonth_Click"/>
        <Button Content="Add One Year" HorizontalAlignment="Left" Margin="400,157,0,0" VerticalAlignment="Top" Height="20" Width="147" Click="btnAddOneYear_Click"/>

        <!--config-->

        <TextBox Text="{Binding MaxRange, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Margin="516,258,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" TextChanged="TextBox_TextChanged"/>
        <Button Content="update" HorizontalAlignment="Left" Margin="556,276,0,0" VerticalAlignment="Top" Click="btnUpdateMaxRange_Click" />
        <Button Content="Reset DB" HorizontalAlignment="Left" Margin="162,127,0,0" VerticalAlignment="Top" Height="40" Width="115" Click="btnResetDB_Click"/>
        <Button Content="Init DB" HorizontalAlignment="Left" Margin="32,127,0,0" VerticalAlignment="Top" Height="40" Width="115"  Click="btnInitDB_Click" RenderTransformOrigin="-0.357,0.423"/>
        <Button Content="Handle Volunteers" HorizontalAlignment="Left" Margin="162,62,0,0" VerticalAlignment="Top" Height="40" Width="115" Click="ButtonVolunteer_Click" />
        <Button Content="Handle Calls" HorizontalAlignment="Left" Margin="32,62,0,0" VerticalAlignment="Top" Height="40" Width="115" Click="ButtonCall_Click"/>

     <Grid Margin="52,264,459,157" RenderTransformOrigin="0.5,0.5">
    <!-- Apply transformations to the grid -->
    <Grid.RenderTransform>
        <TransformGroup>
            <ScaleTransform />
            <SkewTransform />
            <RotateTransform Angle="-0.483" />
            <TranslateTransform />
        </TransformGroup>
    </Grid.RenderTransform>

    <!-- Define the rows of the grid -->
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Define the columns of the grid -->
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="379*" />
        <ColumnDefinition Width="368*" />
    </Grid.ColumnDefinitions>

    <!-- Headers -->
    <Label Content="Status" Grid.Row="0" Grid.Column="0" FontWeight="Bold" Margin="0,0,0,26" Grid.RowSpan="2" /> <!-- Header for the Status column -->
    <Label Content="Sum" Grid.Row="0" Grid.Column="1" FontWeight="Bold" Margin="0,0,0,26" Grid.RowSpan="2" /> <!-- Header for the Sum column -->

    <!-- Row 1: Open -->
    <Label Content="Open" Grid.RowSpan="3" Grid.ColumnSpan="2" Grid.Column="0" Margin="0,22,142,20" /> <!-- Display "Open" status -->
    <Label Content="{Binding CountCall[0]}" Grid.RowSpan="3" Grid.Column="1" Margin="0,16,0,26" /> <!-- Bind count for "Open" status -->

    <!-- Row 2: Treat -->
    <Label Content="Treat" Grid.Row="2" Grid.Column="0" Margin="0,0,0,8" /> <!-- Display "Treat" status -->
    <Label Content="{Binding CountCall[1]}" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" /> <!-- Bind count for "Treat" status -->

    <!-- Row 3: Close -->
    <Label Content="Close" Grid.Row="2" Grid.Column="0" Margin="0,32,0,0" Grid.RowSpan="2" /> <!-- Display "Close" status -->
    <Label Content="{Binding CountCall[2]}" Grid.Row="2" Grid.Column="1" Margin="0,32,0,0" Grid.RowSpan="2" /> <!-- Bind count for "Close" status -->

    <!-- Row 4: Expired -->
    <Label Content="Expired" Grid.Row="3" Grid.Column="0" Margin="0,8,142,-28" Grid.ColumnSpan="2" /> <!-- Display "Expired" status -->
    <Label Content="{Binding CountCall[3]}" Grid.Row="3" Grid.Column="1" Margin="0,8,0,-28" /> <!-- Bind count for "Expired" status -->

    <!-- Row 5: RiskOpen -->
    <Label Content="RiskOpen" Grid.Row="3" Grid.Column="0" Margin="0,40,142,-60" Grid.ColumnSpan="2" /> <!-- Display "RiskOpen" status -->
    <Label Content="{Binding CountCall[4]}" Grid.Row="3" Grid.Column="1" Margin="0,40,0,-60" /> <!-- Bind count for "RiskOpen" status -->

    <!-- Row 6: TreatInRisk -->
    <Label Content="TreatInRisk" Grid.Row="3" Grid.Column="0" Margin="0,72,142,-92" Grid.ColumnSpan="2" /> <!-- Display "TreatInRisk" status -->
    <Label Content="{Binding CountCall[5]}" Grid.Row="3" Grid.Column="1" Margin="0,72,0,-92" /> <!-- Bind count for "TreatInRisk" status -->
</Grid>




    </Grid>

</Window>
