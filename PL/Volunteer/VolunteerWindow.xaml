<!--<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CurrentVolunteer Information" Height="600" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid>
        -->
<!--   Buttons - Exactly as original -->
<!--
        <Button Content="Histoy Calls" Click="btnCallsHistory_Click" 
                HorizontalAlignment="Left" Margin="280,150,0,0" VerticalAlignment="Top" 
                RenderTransformOrigin="0.379,0.314"/>
        <Button Content="Choose Call" Click="btnChooseCall_Click"
                IsEnabled="{Binding CurrentVolunteer, Converter={StaticResource BoolConvertIsCallInProssesKey}}" 
                HorizontalAlignment="Left" Margin="276,245,0,0" VerticalAlignment="Top"/>

    
        <StackPanel Orientation="Horizontal" Margin="10,10,0,0" VerticalAlignment="Top">
            <Label Content="Name:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.FullName}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

  
        <StackPanel Orientation="Horizontal" Margin="10,40,0,0" VerticalAlignment="Top">
            <Label Content="Phone:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.PhoneNumber}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="10,70,0,0" VerticalAlignment="Top">
            <Label Content="Email:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.Email}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="10,100,0,0" VerticalAlignment="Top">
            <Label Content="Current Address:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.FullAddress}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="10,130,541,0" VerticalAlignment="Top">
            <Label Content="Role:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.Job}"  IsReadOnly="True" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

 
        <StackPanel Orientation="Horizontal" Margin="10,160,541,0" VerticalAlignment="Top">
            <Label Content="Active:" FontSize="14" VerticalAlignment="Center"/>
            <CheckBox IsChecked="{Binding CurrentVolunteer.Active}"  RenderTransformOrigin="-2.86,10.806"/>
        </StackPanel>

     
        <StackPanel Orientation="Horizontal" Margin="10,190,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="264">
            <Label Content="Distance:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.MaxReading}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

      
        <StackPanel Orientation="Horizontal" Margin="10,220,529,0" VerticalAlignment="Top">
            <Label Content="Distance Type:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.TypeDistance}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

      
        <Button Content="Update" Click="btnUpdate_Click" 
                HorizontalAlignment="Left" Margin="63,292,0,0" VerticalAlignment="Top"/>

       
        <Button HorizontalAlignment="Left" Margin="22,507,0,0" VerticalAlignment="Top" 
                Click="btnBack_Click">
            <Path Data="M10,0 L0,10 L10,20 L10,14 L20,14 L20,6 L10,6 Z" 
                  Fill="Black" Width="20" Height="20" VerticalAlignment="Top"/>
        </Button>

     
        <StackPanel Orientation="Horizontal" Margin="400,10,0,0" VerticalAlignment="Top">
            <Label Content="Total Handled Calls:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.SumCalls}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="400,40,0,0" VerticalAlignment="Top">
            <Label Content="Total Cancelled Calls:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.SumCanceled}" IsReadOnly="True" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

      
        <StackPanel Orientation="Horizontal" Margin="400,70,0,0" VerticalAlignment="Top">
            <Label Content="Total Expired Calls:" FontSize="14" VerticalAlignment="Center"/>
            <TextBox Text="{Binding CurrentVolunteer.SumExpired}" IsReadOnly="True" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

      
        <StackPanel Orientation="Vertical" 
                    Visibility="{Binding CurrentVolunteer.CallIn, Converter={StaticResource ConvertIsCallInProssesKey}}"  
                    Margin="400,100,0,0" VerticalAlignment="Top" Height="460">
            <TextBox Text="Current Call:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal">
                <Label Content="Call ID:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.IdCall}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Content="Description:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.Description}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Content="Call Type:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.Type}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Content="Call Address:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.FullCallAddress}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Content="Open Time:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.TimeOpen, StringFormat=G}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Content="Max Completion Time:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.MaxTimeToClose, StringFormat=G}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Content="Start treat Time:" FontSize="14" FontWeight="Bold"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.StartTreat, StringFormat=G}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Content="Distance from Volunteer:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.distanceCallVolunteer}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label Content="Status:" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentVolunteer.CallIn.Status}" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Content="Close Call" Click="btnClosed_Call" Width="180" Height="41"/>
            <Button Content="Cancel Call" Click="btnCansel_Call" Width="180" Height="38"/>
        </StackPanel>

        <Image Source="logo.png" HorizontalAlignment="Left" Height="53" 
               Margin="690,14,0,0" VerticalAlignment="Top" Width="100"/>
    </Grid>
</Window>-->




<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Volunteer Information" Height="700" Width="1000"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.3"/>
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <!-- Modern Label Style -->
        <Style x:Key="ModernLabel" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header with Logo -->
        <Border Grid.ColumnSpan="2" Background="#F5F5F5" Height="80" VerticalAlignment="Top">
            <Grid>
                <Image Source="PL\logo.png" HorizontalAlignment="Right" Height="53" Width="100" Margin="0,0,20,0"/>
                <TextBlock Text="Volunteer Information" FontSize="24" VerticalAlignment="Center" Margin="20,0"/>
            </Grid>
        </Border>

        <!-- Left Column - Volunteer Information -->
        <StackPanel Margin="20,100,20,20">
            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <!-- Name -->
                    <DockPanel Margin="0,5">
                        <Label Content="Name:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.FullName}" Style="{StaticResource ModernTextBox}"/>
                    </DockPanel>

                    <!-- Phone -->
                    <DockPanel Margin="0,5">
                        <Label Content="Phone:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.PhoneNumber}" Style="{StaticResource ModernTextBox}"/>
                    </DockPanel>

                    <!-- Email -->
                    <DockPanel Margin="0,5">
                        <Label Content="Email:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.Email}" Style="{StaticResource ModernTextBox}"/>
                    </DockPanel>

                    <!-- Address -->
                    <DockPanel Margin="0,5">
                        <Label Content="Address:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.FullAddress}" Style="{StaticResource ModernTextBox}"/>
                    </DockPanel>

                    <!-- Role -->
                    <DockPanel Margin="0,5">
                        <Label Content="Role:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.Job}" IsReadOnly="True" Style="{StaticResource ModernTextBox}"/>
                    </DockPanel>

                    <!-- Active Status -->
                    <DockPanel Margin="0,5">
                        <Label Content="Active:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <CheckBox IsChecked="{Binding CurrentVolunteer.Active}" VerticalAlignment="Center"/>
                    </DockPanel>

                    <!-- Distance -->
                    <DockPanel Margin="0,5">
                        <Label Content="Distance:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.MaxReading}" Style="{StaticResource ModernTextBox}"/>
                    </DockPanel>

                    <!-- Distance Type -->
                    <DockPanel Margin="0,5">
                        <Label Content="Distance Type:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.TypeDistance}" Style="{StaticResource ModernTextBox}"/>
                    </DockPanel>

                </StackPanel>
           
            </Border>

            <!-- Statistics Section -->
            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Padding="15">
                <StackPanel>
                    <TextBlock Text="Statistics" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <!-- Total Handled Calls -->
                    <DockPanel Margin="0,5">
                        <Label Content="Total Handled:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.SumCalls}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                    </DockPanel>

                    <!-- Total Cancelled Calls -->
                    <DockPanel Margin="0,5">
                        <Label Content="Total Cancelled:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.SumCanceled}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                    </DockPanel>

                    <!-- Total Expired Calls -->
                    <DockPanel Margin="0,5">
                        <Label Content="Total Expired:" Style="{StaticResource ModernLabel}" Width="120"/>
                        <TextBox Text="{Binding CurrentVolunteer.SumExpired}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                    </DockPanel>
                </StackPanel>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Margin="0,20">
                <Button Content="Update" Style="{StaticResource ModernButton}" Click="btnUpdate_Click" Width="100" Margin="0,0,10,0"/>
                <Button Content="History Calls" Style="{StaticResource ModernButton}" Click="btnCallsHistory_Click" Width="100" Margin="0,0,10,0"/>
                <Button Content="Choose Call" Style="{StaticResource ModernButton}" Click="btnChooseCall_Click" Width="100"
                        IsEnabled="{Binding CurrentVolunteer, Converter={StaticResource BoolConvertIsCallInProssesKey}}"/>
            </StackPanel>
        </StackPanel>
      

        <!-- Right Column - Current Call Information -->
        <Border Grid.Column="1" Margin="20,100,20,20"
                Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4"
                Visibility="{Binding CurrentVolunteer.CallIn, Converter={StaticResource ConvertIsCallInProssesKey}}">
            <StackPanel Margin="15">
                <TextBlock Text="Current Call" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <DockPanel Margin="0,5">
                    <Label Content="Call ID:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.IdCall}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <DockPanel Margin="0,5">
                    <Label Content="Description:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.Description}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <DockPanel Margin="0,5">
                    <Label Content="Call Type:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.Type}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <DockPanel Margin="0,5">
                    <Label Content="Call Address:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.FullCallAddress}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <DockPanel Margin="0,5">
                    <Label Content="Open Time:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.TimeOpen, StringFormat=G}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <DockPanel Margin="0,5">
                    <Label Content="Max Completion Time:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.MaxTimeToClose, StringFormat=G}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <DockPanel Margin="0,5">
                    <Label Content="Start Treat Time:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.StartTreat, StringFormat=G}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <DockPanel Margin="0,5">
                    <Label Content="Distance:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.distanceCallVolunteer}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <DockPanel Margin="0,5">
                    <Label Content="Status:" Style="{StaticResource ModernLabel}" Width="150"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallIn.Status}" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                </DockPanel>

                <!-- Call Action Buttons -->
                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <Button Content="Close Call" Click="btnClosed_Call" Style="{StaticResource ModernButton}" Width="120" Margin="0,0,10,0"/>
                    <Button Content="Cancel Call" Click="btnCansel_Call" Style="{StaticResource ModernButton}" Background="#F44336" Width="120"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Back Button -->
        <Button Style="{StaticResource ModernButton}" 
                Background="#757575"
                HorizontalAlignment="Left" 
                VerticalAlignment="Bottom" 
                Margin="20"
                Click="btnBack_Click">
            <Path Data="M10,0 L0,10 L10,20 L10,14 L20,14 L20,6 L10,6 Z" 
                  Fill="White" 
                  Width="17" 
                  Height="20"/>
        </Button>
    </Grid>
</Window>