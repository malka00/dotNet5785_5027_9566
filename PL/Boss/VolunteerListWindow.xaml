<!--<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Title="VolunteerListWindow" Height="640" Width="800" 
    Closed="Window_Closed"
    Loaded="Window_Loaded" >


    -->
<!--External Grid-->
<!--
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        -->
<!--Internal, Upper Grid-->
<!--

        <Grid HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Select a file to sort by" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="121,7,0,0" Grid.Column="1"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource VolunteerInListCollectionKey}}"  
                      SelectionChanged="VolunteerSelector_SelectionChanged" SelectedValue="{Binding VolunteerInList, Mode=TwoWay}" Grid.Column="1" Margin="0,0,284,0"/>

            <Button Content="Filter active volunteer" HorizontalAlignment="Center" VerticalAlignment="Center" Height="43" Width="400" Click="btnFilter_Click" />

        </Grid>


        <Button Content="Add" Grid.Row="2" HorizontalAlignment="Center" Height="50" Width="100" Click="btnAdd_Click"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding VolunteerList}" AutoGenerateColumns="False"  
                 IsReadOnly="True" SelectedItem="{Binding SelectedVolunteer}" MouseDoubleClick="dtgList_MouseDoubleClick">



            -->
<!-- הגדרת סגנון לכותרות -->
<!--
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="AliceBlue"/>
                    <Setter Property="Foreground" Value="#0D1821"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>

                <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="*"/>
                <DataGridTextColumn Binding="{Binding FullName}" Header="Name" Width="*"/>

                <DataGridTemplateColumn Header="Is Active">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsEnabled="False" IsChecked="{Binding Active}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Binding="{Binding SumCalls}" Header="SumCalls" Width="*"/>
                <DataGridTextColumn Binding="{Binding SumCanceled}" Header="SumCanceled" Width="*"/>
                <DataGridTextColumn Binding="{Binding SumExpired}" Header="SumExpired" Width="*"/>
                <DataGridTextColumn Binding="{Binding IdCall}" Header="IdCall" Width="*"/>

                <DataGridTemplateColumn Header="Call Type">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CType}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CType}" Value="Puncture">
                                                <Setter Property="Foreground" Value="Red" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding CType}" Value="Cables">
                                                <Setter Property="Foreground" Value="Blue" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding CType}" Value="LockedCar">
                                                <Setter Property="Foreground" Value="Green" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="btnDelete_Click" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>
        <Button HorizontalAlignment="Left" Margin="10,20,0,0" Grid.Row="2" VerticalAlignment="Top" Click="btnBack_Click">
            <Path Data="M10,0 L0,10 L10,20 L10,14 L20,14 L20,6 L10,6 Z" 
          Fill="Black" 
          Width="20" 
          Height="20" 
          VerticalAlignment="Center"/>
        </Button>

        <Image Source ="C:\Users\efrat\Documents\מיני פרויקט\logo.png" HorizontalAlignment="Left" Height="53" Margin="700,-1,0,0" VerticalAlignment="Top" Width="100" Grid.RowSpan="2"/>

    </Grid>
</Window>-->

<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Title="VolunteerListWindow" Height="640" Width="800" 
        Closed="Window_Closed"
        Loaded="Window_Loaded"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="379*"/>
            <ColumnDefinition Width="21*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Height="auto" Grid.Row="0" Width="auto" Margin="0,10,20,0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Header Section -->
                <Border Background="#F5F5F5" Grid.Row="0" Padding="10">
                    <Grid>
                        <Image Source="C:\Users\efrat\Documents\מיני פרויקט\PL\logo.png"  
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Top" 
                   Width="95" 
                   Height="Auto" 
                   Margin="0,-11,-408,0"/>
                    </Grid>
                </Border>
            </Grid>



            <Label Content="Sort by:" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Center" 
                   Margin="105,0,0,0" 
                   Grid.Column="1"
                   FontFamily="Trebuchet MS"
                   FontSize="15"/>

            <ComboBox Style="{StaticResource ModernComboBox}"
                      ItemsSource="{Binding Source={StaticResource VolunteerInListCollectionKey}}"  
                      SelectionChanged="VolunteerSelector_SelectionChanged" 
                      SelectedValue="{Binding VolunteerInList, Mode=TwoWay}" 
                      HorizontalAlignment="Left" 
                      Width="170" 
                      Grid.ColumnSpan="2" 
                      Margin="325,1,0,0"/>

            <Button Content="Filter active volunteer"
                    FontFamily="Trebuchet MS"
                    Style="{StaticResource ModernButton}"
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Center" 
                    Height="40" 
                    Width="250" 
                    Click="btnFilter_Click" 
                    Margin="11,0,0,0"/>
        </Grid>

        <Button Content="Add" 
          Grid.Row="2" 
          Style="{StaticResource ModernButton}"
          HorizontalAlignment="Center" 
          Height="50" 
          Width="100" 
          Margin="0,10" 
          Click="btnAdd_Click"/>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding VolunteerList}" 
                  AutoGenerateColumns="False"  
                  IsReadOnly="True" 
                  SelectedItem="{Binding SelectedVolunteer}" 
                  MouseDoubleClick="dtgList_MouseDoubleClick"
                  Background="White"
                  BorderBrush="#E0E0E0"
                  BorderThickness="1"
                  RowBackground="White"
                  AlternatingRowBackground="#F5F5F5"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#E0E0E0"
                  RowHeight="35"
                  Margin="10,10,10,10" Grid.ColumnSpan="2" SelectionChanged="DataGrid_SelectionChanged">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2196F3"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type DataGridCell}">
                                <Border Padding="{TemplateBinding Padding}" 
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="*"/>
                <DataGridTextColumn Binding="{Binding FullName}" Header="Name" Width="*"/>

                <DataGridTemplateColumn Header="Is Active">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsEnabled="False" 
                                    IsChecked="{Binding Active}"
                                    HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Binding="{Binding SumCalls}" Header="SumCalls" Width="*"/>
                <DataGridTextColumn Binding="{Binding SumCanceled}" Header="SumCanceled" Width="*"/>
                <DataGridTextColumn Binding="{Binding SumExpired}" Header="SumExpired" Width="*"/>
                <DataGridTextColumn Binding="{Binding IdCall}" Header="IdCall" Width="*"/>

                <DataGridTemplateColumn Header="Call Type">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CType}" HorizontalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CType}" Value="Puncture">
                                                <Setter Property="Foreground" Value="#F44336"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding CType}" Value="Cables">
                                                <Setter Property="Foreground" Value="#2196F3"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding CType}" Value="LockedCar">
                                                <Setter Property="Foreground" Value="#4CAF50"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button 
                                Content="Delete" 
                                Click="btnDelete_Click"
                                Height="23"
                                Width="70">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                        <Setter Property="Background" Value="#F44336"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#D32F2F"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Back Button -->
        <Button Grid.Row="2"
   HorizontalAlignment="Left" 
   VerticalAlignment="Center"
   Margin="20,0,0,0"
   Style="{StaticResource ModernButton}"
   Background="#757575"
   Click="btnBack_Click" Height="30" Width="47">
            <Path Data="M10,0 L0,10 L10,20 L10,14 L20,14 L20,6 L10,6 Z" 
     Fill="White" 
     Width="17" 
     Height="20" 
     Stretch="Fill"/>
        </Button>
    </Grid>
</Window>
